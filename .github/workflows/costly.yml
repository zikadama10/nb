name: blank

on:
  push:

jobs:
  hurry:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        hatred: ["accelerate", "minor", "option", "earnest", "creative"]
        lend: ["put", "tolerance", "powder", "central"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar AS
